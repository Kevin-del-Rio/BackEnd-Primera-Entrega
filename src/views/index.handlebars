<div>
    <h1>Hola! {{ name}}, que gusto verte!</h1>

    {{#if isAdmin}}
        {{#each food}}
             <div>
                <p>{{this.name}}</p>
                <p>{{this.precio}}</p>
             </div>
        {{/each}}
        {{else}}
           <p>No tienes permisos para ver la lista de comidas</p>
    {{/if}}

</div>